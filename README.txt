README
Code written in Python and is in a single file, Hangman.py. Attached along with
this is a word_list that needs to be in the same directory as the python file
for the program to work.

The required library to install is 'requests' which can be installed with -
pip install requests.

To run the code, do python Hangman.py
